# UI/UX-аудит лендинга — 20.08.2026

Автор: Дмитрий (ветка `dima`). Проверено: десктоп 1280×800 и мобильный 375×812,
светлая тема, навигация, форма, вес страницы, контраст (WCAG), консоль и сеть.

## P0 — блокирует запуск рекламы

1. **Математика пакетов сломана ценой лендинга.** В услугах «Лендинги и сайты —
   от 12 000 ₽», но пакет «Бот + воронка» стоит 42 000 ₽ с подписью «выгода 6 000 ₽».
   Считаем: лендинг 12 000 + бот 16 000 = 28 000 — пакет получился **дороже**
   отдельных услуг на 14 000, и «выгода» превращается в обман. Внимательный клиент
   это посчитает. Либо возвращаем «от 32 000 ₽», либо пересчитываем пакеты и «выгоды».
   Заодно: в schema.org разметке цена лендинга осталась старая (32 000) — рассинхрон.

2. **Заявка теряется, если человек не нажал «отправить» в Telegram.** Сабмит формы
   открывает t.me с предзаполненным текстом — если клиент закрыл Telegram, заявки нет
   нигде. Плюс это неожиданное поведение: жал «Отправить заявку», а попал в мессенджер.
   Нужен бэкенд формы (вебхук на бота — задача Дмитрия), t.me оставить запасным путём.

3. **Метрика не подключена.** В коде цели `lead_submit`, `audit_cta`, `telegram_click`
   готовы, но счётчика нет (`NEXUS_METRIKA_ID` не задан) — запускать Директ без него
   нельзя, оптимизироваться будет не на чем.

## P1 — до запуска желательно

4. **Модальное окно** (решение планёрки 17.08: через 15–20 сек, ненавязчиво) — не
   реализовано. За Виктором.
5. **Блока «Кейсы» нет** (решение планёрки: хотя бы каркас). За Виктором.
6. **Контраст подписей в карточках услуг** («Старт · с интеграциями · …») — 3.4:1
   при норме 4.5:1 в светлой теме. Затемнить цвет `service__note` на пару ступеней.
7. **Канонический URL и OG указывают на nexus-lab-landing.vercel.app.** Для рекламы
   и SEO нужен свой домен (lidogeneratornaya.ru был свободен — проверить и купить),
   после покупки заменить canonical/OG/schema разом.
8. **Кнопки-иконки в шапке** (тема/язык) меньше рекомендованных 40–44px тап-таргета
   на мобильном.

## P2 — мелочи и наблюдения

9. Атрибуты `data-i18n` остались по всей разметке, а `js/i18n.js` удалён — либо
   вернуть EN-версию, либо вычистить атрибуты, чтобы не путали.
10. В hero и стики-баре CTA ведёт на блок разбора, откуда вторая кнопка ведёт на
    форму — путь в два шага. Ок для прогрева, но стоит проверить на реальных данных
    Метрики (карта кликов), не теряем ли людей между шагами.
11. FAQ-аккордеоны позволяют открыть все сразу — норм, но при открытии длинного
    ответа секция-экран растягивается; проверить глазами, что не дёргается.
12. Тёмная тема: проверить контраст тем же чек-листом после правок палитры.

## Что уже хорошо (не трогать)

- Вес страницы 126 КБ со шрифтами — быстрее 90% лендингов; самое тяжёлое — шрифты (37 КБ).
- Все якоря навигации ведут на существующие блоки, битых ссылок и 404 нет.
- Иерархия заголовков корректная (один H1, логичные H2/H3), у всех картинок есть alt.
- Кастомный селект в форме доступен с клавиатуры (стрелки, Enter, Esc) — редкость.
- Форма из 3 полей — низкий порог; контраст основного текста и кнопок — с запасом.
- Секции-экраны после правок ровные: 1.0 высоты вьюпорта (макс. отклонение 4%).
